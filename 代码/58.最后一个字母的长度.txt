class Solution {
    public int lengthOfLastWord(String s) {

        int len = s.length();
        if(len == 0) return 0;

        String[] str = s.split(" ");
        return (str.length == 0)?0:str[str.length - 1].length();

    }
}